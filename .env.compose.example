# ===================================================================
# Unified Docker Compose Environment Variables for ZapWA
# ===================================================================

# -------------------------------------------------------------------
# PostgreSQL Database (db service)
# These variables configure the database user, password, and name.
# -------------------------------------------------------------------
POSTGRES_DB=zapwa
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mysecretpassword

# -------------------------------------------------------------------
# Backend Server (server service)
# These variables are used by the Hono server.
# -------------------------------------------------------------------
# Full database connection URL for Drizzle ORM.
# Note: 'db' is the hostname of the database service inside the Docker network.
DATABASE_URL='postgresql://myuser:mysecretpassword@db:5432/zapwa'

# Secret key for better-auth session management. Generate a new one for production.
# You can generate one with: openssl rand -base64 32
BETTER_AUTH_SECRET='replace-with-your-strong-secret-key'

# API key for OpenAI services.
OPENAI_API_KEY='your_openai_api_key_here'

# URL of the client application for CORS configuration.
# The server needs to know which client origin is allowed to connect.
# 'http://localhost:8080' is the address of the client service exposed by Docker.
CLIENT_URL='http://localhost:8080'

# -------------------------------------------------------------------
# Frontend Client (client service)
# This variable tells the React app where to find the backend API.
# -------------------------------------------------------------------
# The server URL that the Vite/React client will make API requests to.
# 'http://localhost:3000' is the address of the server service exposed by Docker.
VITE_SERVER_URL='http://localhost:3000'
